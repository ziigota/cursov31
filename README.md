## 📖 Описание проекта

Это курсовая работа по статистическому моделированию, которая анализирует датасет из **232,725 треков Spotify** и строит модели машинного обучения для предсказания популярности музыки на основе аудио-характеристик.

**Основная идея:** Можно ли предсказать популярность трека, зная только его аудио-характеристики (темп, энергичность, танцевальность и т.д.)?

**Результат:** Модель Random Forest достигает **R² = 0.42**, что означает, что 42% вариации популярности можно объяснить аудио-характеристиками.

---

## ✨ Функционал

### 📊 Статистический анализ
- Анализ распределений (громкость, темп, танцевальность)
- Корреляционный анализ признаков
- Сравнение жанров (Pop, Rap, Rock, Classical, Anime)
- Визуализация данных (гистограммы, scatter-plots, heatmaps)

### 🤖 Машинное обучение
- Обучение двух моделей регрессии:
  - **Linear Regression** (базовая линейная модель)
  - **Random Forest Regressor** (ансамбль из 100 деревьев)
- Сравнение моделей по метрикам: R², RMSE, MAE
- Анализ важности признаков (Feature Importance)

### 🎯 Предсказание популярности
- Интерактивная форма для ввода характеристик трека
- Мгновенное предсказание популярности (0-100)
- Интерпретация результатов и рекомендации

---

## 🛠 Технологии

### Backend
- **Python 3.8+** — основной язык
- **FastAPI** — веб-фреймворк для API
- **scikit-learn** — модели машинного обучения
- **pandas** — обработка данных
- **NumPy** — численные вычисления
- **matplotlib / seaborn** — визуализация

### Frontend
- **HTML5 / CSS3** — структура и стили
- **JavaScript (ES6)** — логика интерфейса
- **jQuery** — AJAX-запросы
- **Vanilla JS** — компонентная архитектура

---

## 🚀 Установка и запуск

### Предварительные требования

- **Python 3.8 или выше**
- **pip** (менеджер пакетов Python)
- **Git** (для клонирования репозитория)

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/your-username/spotify-analysis.git
cd spotify-analysis
```

### Шаг 2: Создание виртуального окружения

#### Windows:
```bash
# Создать venv
python -m venv venv

# Активировать
venv\Scripts\activate
```

#### macOS / Linux:
```bash
# Создать venv
python3 -m venv venv

# Активировать
source venv/bin/activate
```

**Проверка:** После активации в начале командной строки должно появиться `(venv)`.

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

**Что устанавливается:**
- fastapi
- uvicorn[standard]
- pandas
- numpy
- scikit-learn
- matplotlib
- seaborn

### Шаг 4: Подготовка данных

1. Скачайте датасет **SpotifyFeatures.csv** с [Kaggle](https://www.kaggle.com/datasets/zaheenhamidani/ultimate-spotify-tracks-db)
2. Поместите файл в папку `data/`:

```
spotify-analysis/
├── data/
│   └── SpotifyFeatures.csv  ← сюда
├── backend/
├── frontend/
└── ...
```

### Шаг 5: Запуск сервера

```bash
# Из корневой папки проекта
python main.py
```

**Вывод в консоли:**
```
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     Application startup complete.
✓ Датасет загружен: 232,725 строк × 18 колонок
```

### Шаг 6: Открытие приложения

Откройте в браузере:
```
http://localhost:8000
```

---

## 📁 Структура проекта

```
spotify-analysis/
│
├── data/                          # Данные
│   └── SpotifyFeatures.csv        # Датасет Spotify (не в репозитории)
│
├── backend/                       # Серверная часть
│   ├── config.py                  # Конфигурация (константы)
│   ├── services/                  # Бизнес-логика
│   │   ├── data_service.py        # Загрузка и очистка данных
│   │   ├── analysis_service.py    # Статистический анализ
│   │   ├── model_service.py       # Машинное обучение
│   │   └── plot_service.py        # Генерация графиков
│   └── routes/                    # API эндпоинты
│       ├── data.py                # /data/info
│       ├── analysis.py            # /analysis/*
│       ├── model.py               # /model/*
│       └── plots.py               # /plots/*
│
├── frontend/                      # Клиентская часть
│   ├── index.html                 # Главная страница
│   ├── css/                       # Стили
│   │   ├── main.css
│   │   ├── components.css
│   │   └── navigation.css
│   └── js/                        # JavaScript
│       ├── config.js              # Конфигурация API
│       ├── api.js                 # HTTP-клиент
│       ├── utils.js               # Утилиты UI
│       ├── main.js                # Инициализация
│       └── components/            # UI компоненты
│           ├── dataInfo.js
│           ├── distributions.js
│           ├── correlations.js
│           ├── genres.js
│           ├── plots.js
│           ├── model.js
│           └── predict.js
│
├── main.py                        # Точка входа (запуск сервера)
├── requirements.txt               # Зависимости Python
├── README.md                      # Документация
└── .gitignore                     # Игнорируемые файлы
```

---
### Быстрый старт (5 минут)

```bash
# 1. Клонировать репозиторий
git clone 
cd spotify-analysis

# 2. Создать venv
python -m venv venv

# 3. Активировать (Windows)
venv\Scripts\activate

# 3. Активировать (macOS/Linux)
source venv/bin/activate

# 4. Установить зависимости
pip install -r requirements.txt

# 5. Положить датасет в data/SpotifyFeatures.csv

# 6. Запустить
python main.py

# 7. Открыть http://localhost:8000
```

### Что изучить для понимания проекта

**Backend (Python):**
- `backend/services/model_service.py` — обучение моделей
- `backend/routes/model.py` — API эндпоинты
- `main.py` — запуск сервера

**Frontend (JavaScript):**
- `frontend/js/components/model.js` — UI обучения модели
- `frontend/js/components/predict.js` — UI предсказания
- `frontend/index.html` — структура страницы

**Ключевые концепции:**
- Регрессия (Linear Regression, Random Forest)
- Метрики качества (R², RMSE, MAE)
- Feature Engineering
- Train/Test Split
- RESTful API
